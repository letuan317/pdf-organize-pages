version: "3.8"

services:
  pdf-organizer:
    build: .
    container_name: pdf-organizer
    ports:
      - "5000:5000"
    volumes:
      - .:/app
    environment:
      - FLASK_ENV=development
